server:
  listen_addr: "127.0.0.1"
  listen_port: 18005
  log_level: "error"
routes:
  - host: "localhost"
    locations:
      - path: "/"
        upstreams: ["127.0.0.1:8081"]
        exact_match: false
